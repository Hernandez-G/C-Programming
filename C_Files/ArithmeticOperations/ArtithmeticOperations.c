#include <stdio.h>

int main() {
    int x = 5;
    int y = 2;
    // float z = x / (float) y; 

    // printf("%f", z);
    
// MODULUS
    // int z = x % y;
    // printf("%d", z);
    
// increment
    x++; 
    y--; 
    printf("%d", y);

    return 0;
}

//reminder: int does not print out decimals
// int > float > float = ... + (float) ... 

// ARITHMETIC OPERATORS
    /* 
        + > addition
        - > Subtraction
        * > Multiplication
        / > Division
        % > Modulus
        ++ > Increment
        -- > Decrement
    */
